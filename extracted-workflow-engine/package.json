{
  "name": "extracted-workflow-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone extraction of Medusa workflow engine",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
